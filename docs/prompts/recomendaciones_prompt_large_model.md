Busca tú mismo en los datos {nivel_txt} (usa las tablas ya indicadas arriba) y dame EXACTAMENTE
3 recomendaciones de negocio ganadero, independientes entre sí (sobre regiones distintas cuando
sea posible). Formato de cada línea: empieza con un emoji (🔴 alarma grave, 🟠 aviso, ✅ sin
alarma, 👀 vigilar, 🎯 estrategia, 🏆 referencia positiva) + 'Las ganaderías Limousin de
[región]...' + al menos una cifra exacta en **negrita** + si aplica, '**Recomendación:** ...'.
AL MENOS 2 de las 3 recomendaciones deben ser una COMPARATIVA explícita entre dos regiones
(ej. 'Las ganaderías Limousin de X paren un **Y%**, frente al **Z%** de las de W'), no solo un
dato aislado de una región suelta — cita ambas cifras y ambos nombres, y la diferencia entre
ellas. La tercera puede ser un dato individual si aporta algo distinto (p.ej. un hito o alerta
puntual).

REGLA DE NIVEL: hay DOS tablas (una por comunidad autónoma, otra por provincia) — estás
trabajando {nivel_txt}, así que toda comparación debe ser entre dos filas de esa misma tabla, sin
mezclar una comunidad autónoma con una provincia en la misma frase.

Para esta tanda en particular: {variante}.

Puedes usar cualquier columna de la tabla, incluida pct_menos_365d (% de intervalos que fueron
≤365 días — un valor alto es BUENO, no confundir con el % de intervalos largos). Verifica cada
cifra contra la tabla antes de escribirla y confirma cuál región es realmente la peor/mejor
comparándola con el resto del conjunto, no solo con la primera que te parezca destacable — no
inventes ni redondees de más.

Devuelve solo las 3 líneas, una por bloque, sin numerarlas, sin introducción ni cierre. Escribe
cada línea como una frase completa y natural — no uses puntos suspensivos ('...') como relleno.
