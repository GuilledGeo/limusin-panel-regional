Busca tú mismo en los datos {nivel_txt} (usa las tablas ya indicadas arriba) y dame EXACTAMENTE
3 recomendaciones de negocio ganadero, independientes entre sí (sobre regiones distintas cuando
sea posible). Formato de cada línea: empieza con un emoji (🔴 alarma grave, 🟠 aviso, ✅ sin
alarma, 👀 vigilar, 🎯 estrategia, 🏆 referencia positiva) + 'Las ganaderías Limousin de
[región]...' + al menos una cifra exacta en **negrita** + si aplica, '**Recomendación:** ...'.
AL MENOS 2 de las 3 recomendaciones deben ser una COMPARATIVA explícita entre dos regiones
(ej. 'Las ganaderías Limousin de X paren un **Y%**, frente al **Z%** de las de W'), no solo un
dato aislado de una región suelta — cita ambas cifras y ambos nombres, y la diferencia entre
ellas. La tercera puede ser un dato individual si aporta algo distinto (p.ej. un hito o alerta
puntual).

REGLA ESTRICTA DE NIVEL: hay DOS tablas (una por comunidad autónoma, otra por provincia) — estás
trabajando {nivel_txt}, así que TODA comparación debe ser entre dos filas de ESA MISMA tabla
(dos comunidades autónomas entre sí, o dos provincias entre sí). NUNCA compares una comunidad
autónoma con una provincia en la misma frase, aunque el nombre se parezca (p.ej. Madrid como
CCAA y Madrid como provincia son la misma fila, pero Álava es una PROVINCIA, no una CCAA — no la
uses si estás trabajando a nivel comunidad autónoma).

Para esta tanda en particular: {variante}.

IMPORTANTE — NO menciones ni cites la columna pct_menos_365d (el % de intervalos ≤365 días) bajo
ningún concepto: es una cifra fácil de malinterpretar (confundir con el % de intervalos largos,
que es justo lo contrario) y en pruebas anteriores se ha citado al revés varias veces. Usa
ÚNICAMENTE índice de parición (%) e intervalo entre partos (días) para tus 3 recomendaciones,
que son inequívocos.

VERIFICA cada cifra contra la tabla antes de escribirla — no inventes, no redondees de más, no
confundas cuál región es la peor/mejor.

ANTES de escribir cada línea final, haz este chequeo en silencio (no lo muestres, es solo para
ti): (1) relee la fila completa de la región elegida en la tabla — todos sus valores, no solo el
que vas a citar; (2) confirma comparándola con al menos 2-3 filas más que de verdad es la peor/
mejor en lo que vas a decir, no solo la primera que te ha parecido destacable; (3) confirma qué
significa exactamente cada columna (relee la definición de pct_menos_365d de arriba) antes de
calificar un valor como bueno o malo. Si al hacer este chequeo ves que la cifra no dice lo que
ibas a escribir, cambia la frase — no la fuerces. Solo cuando hayas verificado los tres puntos,
escribe la línea final.

Devuelve solo las 3 líneas ya verificadas, una por bloque, sin numerarlas, sin mostrar el
chequeo ni añadir introducción/cierre. Escribe cada línea como una frase completa y natural —
NUNCA uses puntos suspensivos ('...') como relleno entre el nombre de la región y la cifra ni en
ningún otro punto de la frase.
